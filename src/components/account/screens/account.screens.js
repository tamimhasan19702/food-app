/** @format */

import React from "react";

import { AccountBackground, AccountView } from "../components/account.styles";

export const Account = () => {
  return (
    <AccountBackground>
      <AccountView />
    </AccountBackground>
  );
};
